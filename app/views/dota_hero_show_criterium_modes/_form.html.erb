<%= form_for(@dota_hero_show_criterium_mode) do |f| %>
  <% if @dota_hero_show_criterium_mode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dota_hero_show_criterium_mode.errors.count, "error") %> prohibited this dota_hero_show_criterium_mode from being saved:</h2>

      <ul>
      <% @dota_hero_show_criterium_mode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
