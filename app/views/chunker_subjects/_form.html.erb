<%= form_for(@chunker_subject) do |f| %>
  <% if @chunker_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chunker_subject.errors.count, "error") %> prohibited this chunker_subject from being saved:</h2>

      <ul>
      <% @chunker_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
